#include<iostream>

using namespace std;

#define rep(i,n) for(i=0;i<(n);i++)
#define For(i,a,b) for(i=(a);i<(b);i++)

int main(){
  int i,j,k;
  cin >> i >> j;
  cout << (j-i)+j << endl;
  return 0;
}
